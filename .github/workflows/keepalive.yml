name: Keep HF Space Alive
on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时触发一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -L https://cwk14-gold-manus-ai.hf.space/
